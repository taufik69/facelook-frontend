import React from "react";
const logoimg = "/images/logo.png";
const Logo = () => {
  return (
    <div>
      <img src={logoimg} alt={`logo missign on this part ${logoimg}`} />
    </div>
  );
};

export default Logo;
